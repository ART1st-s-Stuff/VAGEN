env1:
    env_name: navigation
    env_config:
        render_mode: vision
        prompt_format: grounding_worldmodeling
        use_state_reward: false
        eval_set: base
    train_size: 4
    test_size: 1

env2:
    env_name: navigation
    env_config:
        render_mode: vision
        prompt_format: grounding_worldmodeling
        use_state_reward: false
        eval_set: common_sense
    train_size: 4
    test_size: 1
